<!--
  Styles et ressources globales (convertisseurs, styles simples).
  Ce dictionnaire est mergé dans App.xaml → accessible partout en StaticResource.
-->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:conv="clr-namespace:Ecauspacine.Wpf.Converters">
    <!-- Converter disponible globalement -->
    <conv:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>

    <!-- Styles basiques -->
    <Style TargetType="Window">
        <Setter Property="Background" Value="{StaticResource BackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
    </Style>

    <Style TargetType="Button">
        <Setter Property="Padding" Value="8,4"/>
    </Style>
</ResourceDictionary>
