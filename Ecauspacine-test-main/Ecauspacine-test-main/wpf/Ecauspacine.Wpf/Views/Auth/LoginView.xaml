<UserControl x:Class="Ecauspacine.Wpf.Views.Auth.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignWidth="600"
             d:DesignHeight="400">
    <Grid Background="{StaticResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Row="1"
                Grid.Column="1"
                Padding="40"
                CornerRadius="12"
                Background="#222C3C"
                BorderBrush="#334155"
                BorderThickness="1">
            <StackPanel Width="320">
                <TextBlock Text="EDDS - Connexion"
                           FontSize="22"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,16" />

                <StackPanel Margin="0,0,0,12">
                    <TextBlock Text="Nom d'utilisateur" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                             Height="32"
                             Background="#1F2937"
                             BorderBrush="#334155"
                             Foreground="{StaticResource ForegroundBrush}" />
                </StackPanel>

                <StackPanel Margin="0,0,0,12">
                    <TextBlock Text="Mot de passe" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <PasswordBox x:Name="PasswordBox"
                                 Height="32"
                                 Background="#1F2937"
                                 BorderBrush="#334155"
                                 Foreground="{StaticResource ForegroundBrush}"
                                 PasswordChanged="OnPasswordChanged" />
                </StackPanel>

                <Button Content="Se connecter"
                        Command="{Binding LoginCommand}"
                        Height="36"
                        Background="{StaticResource AccentBrush}"
                        Foreground="White"
                        FontWeight="SemiBold"
                        Margin="0,0,0,12" />

                <TextBlock Text="{Binding ErrorMessage}"
                           Foreground="#F87171"
                           TextWrapping="Wrap"
                           Margin="0,0,0,12"
                           Visibility="{Binding ErrorMessage, Converter={StaticResource StringNullOrEmptyToVisibilityConverter}}" />

                <ProgressBar IsIndeterminate="True"
                             Height="6"
                             Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
